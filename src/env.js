
const rootDir= "./assets";

const ENV= {

	RESOURCE_DIR: `${rootDir}/`,

	LEVELS_DIR: `${rootDir}/levels/`,

	AUDIO_DIR: `${rootDir}/sounds/`,
	VOLUME: 50,

	SPRITESHEET_DIR: `${rootDir}/spritesheets/`,

	BRICK_TOP: 70,
	BRICK_LEFT: 55,
	WALL_TOP: 50
};

export default ENV;